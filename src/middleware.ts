import { withAuth } from "next-auth/middleware";

export default withAuth({
    pages: {
        signIn: "/", // ⛔ ถ้ายังไม่ Login ให้ดีดกลับมาหน้าแรก (Landing Page)
    },
});

export const config = {
    // 🔒 ระบุ path ที่ต้องการ "ล็อก" (ห้ามเข้าถ้าไม่ Login)
    // เราจะไม่ใส่ '/overlay/:path*' ในนี้ เพื่อให้เข้าได้อิสระ
    matcher: [
        "/home/:path*",
        "/write/:path*",
        "/archived/:path*",
        // เพิ่ม path อื่นๆ ที่เป็น Private ได้ที่นี่
    ],
};